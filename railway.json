[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "flask init-db && gunicorn --bind 0.0.0.0:$PORT --workers 2 run:app"
healthcheckPath = "/login"
restartPolicyType = "ON_FAILURE"
